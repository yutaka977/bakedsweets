<h3>投稿検索</h3>
<%= search_form_for @q  do |f| %>
    <%= f.label :body_cont, '店名' %>
    <%= f.search_field :body_cont %>
    <%= f.label :type_id_eq, '種類' %>
    <%= f.collection_select(:type_id_eq, Type.all, :id, :name, { prompt: '選択してください' }, { class: 'form-select' }) %>
    <%= f.submit "検索" %></p>
<% end %>
<h3>投稿一覧</h3>
<div class="wrapper">
<div class="tweets-container">
  <% @tweets.reverse_each do |t| %>
    <div class="tweet">  
        <% if t.images.attached? %>
          <% image = t.images.first %>
      <%= link_to  tweet_path(t.id) do %>
      <div class='image-field'><%= image_tag image,width: 330, height: 240, 
    class: "img-resize" %></div>
        <% end %>
        <% end %>
      種類: <%= t.type.name %>
      <br>
      価格: <%= t.cost %>円
      <br>
      美味しさ:<%= t.taste %>
      <br>
いいね 
          <% if user_signed_in? %>
      <% if current_user.already_liked?(t) %>
        <%= link_to tweet_like_path(id: t.id, tweet_id: t.id), data: { turbo_method: :delete } do %>
            <i class="fas fa-heart"></i><%= t.likes.count %>
        <% end %>
      <% else %>
        <%= link_to tweet_likes_path(id: t.id, tweet_id: t.id), data: { turbo_method: :post }  do %>
            <i class="far fa-heart"></i><%= t.likes.count %>
        <% end %>
      <% end %>
    <% else %>
        <i class="far fa-heart"></i><%= t.likes.count %>
    <% end %>
    </div>
  <% end %>
</div>
</div>



<div id='map'></div>

<style>
#map {
  height: 600px;
  width: 100%;
}
</style>

<!-- js部分 -->
<script>
    function initMap() {

      //初期表示位置
      let latlng = new google.maps.LatLng(38.60, 139.5);
      //デザイン
      let styles = [
           {
            stylers: [
             { "saturation": 0 },
             { "lightness": 0 }
            ]
           }
          ];

      let map = new google.maps.Map(document.getElementById('map'), {
          zoom: 5.5,
          styles: styles,
          center: latlng
      });
      let transitLayer = new google.maps.TransitLayer();
      transitLayer.setMap(map);

//複数マーカー ここから
      <% @tweets.each do |tweet|%>
        ( function() { 
          let markerLatLng = { lat: <%= tweet.lat %>, lng: <%= tweet.lng %> }; // 緯度経度のデータ作成
          let marker = new google.maps.Marker({ 
            position: markerLatLng, 
            map: map 
          });
//マーカーをクリックしたとき、詳細情報を表示
          let infowindow = new google.maps.InfoWindow({
            position: markerLatLng,
            content: "<a href='<%= tweet_url(tweet.id) %>' target='_blank'><%= tweet.body %></a>"
          }); //ここで詳細ページへのリンクを表示させる
          marker.addListener('click', function() {
            infowindow.open(map, marker);
          });

       })();
      <% end %>
      //複数マーカー ここまで  
  }
</script>

<script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['YOUR_API_KEY'] %>&callback=initMap" async defer></script>



